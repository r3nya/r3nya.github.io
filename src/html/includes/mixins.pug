mixin link(title, icon, url)
  a.flex.items-center.text-2xl.font-medium.text-palette-5-light.group(
    href=url,
    class="dark:text-palette-5-dark hover:text-palette-6-light dark:hover:text-palette-6-dark"
  )
    i(class=`icon-${icon} w-8 mr-2 flex-shrink-0`)
    span(class="group-hover:underline")= title
