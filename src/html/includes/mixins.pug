mixin link(title, icon, url)
  .link(role='link' class="text-lg md:text-xl lg:text-2xl xl:text-3xl mb-2 md:mb-4")
    a(href=url class="text-palette-5 hover:text-palette-6 hover:underline")
      i(class=`icon-${icon} inline-block w-6 md:w-8 lg:w-10 xl:w-12 mr-2`)
      = title
