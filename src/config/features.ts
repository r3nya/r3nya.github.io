export interface HomePageFeatures {
  bio: boolean;
  contact: boolean;
  cv: boolean;
  links: boolean;
  themeToggle: boolean;
}

export const homePageFeatures: HomePageFeatures = {
  bio: false,
  contact: false,
  cv: false,
  links: true,
  themeToggle: true,
};
